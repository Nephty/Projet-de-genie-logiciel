package back.user;

public class Hasher {
    public static String hash(String s) {
        // TODO : back-end : implement hashing function to check if the hash of the current password that we retrieve is the same as the hash of the new password (change password scene)
        return s;
    }
}
